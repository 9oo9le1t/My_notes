strace ls  #показывает системные вызовы (syscalls)

ldd /bin/ls  #какие библиотеки программа подтягивает для работы

sudo tcpdump -i lo -A port 4444 # слушаем локальный трафик на порту 4444 в текстовом виде
